<Window x:Class="Minecraft_Server_Maker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minecraft_Server_Maker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel  VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Select Server Path: " />

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Border BorderBrush="LightGray" BorderThickness="1" Padding="8" CornerRadius="3">
                    <TextBlock Text="{Binding JarPathDisplay}" VerticalAlignment="Center"/>
                </Border>
                <Button Grid.Column="1"
                        Content="Select .jar"
                        Command="{Binding SelectJarCommand}"
                        Margin="10,0,0,0" Padding="10,5"/>
                    </Grid>
            
            <Label Content="Enter the Server Name: " />
                <StackPanel>
                <TextBox Text="{Binding ServerName, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Width="200" Height="30" />
            <TextBlock Text="{Binding ServerName}" Margin="10"  FontSize="18" Foreground="Green" />
                </StackPanel>
        </StackPanel>
    </Grid>
</Window>